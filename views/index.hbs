{{!< default}}
<div id="container">
	<header>
		<h1>{{title}}{{^title}}Dashee{{/title}}</h1>
	</header>
	<div id="blocks">
		{{#blocks}}
		<!-- block-{{id}} -->
		{{{html}}}
		{{/blocks}}
		{{^blocks}}
		<!-- No Blocks Found -->
		{{/blocks}}
	</div>
</div>